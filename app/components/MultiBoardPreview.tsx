// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _renderBoard = (_board: {
  id: string;
  layout: {
    images: Array<{
      url: string;
      position: { x: number; y: number; w: number; h: number };
      rotation: number;
    }>;
  };
  printSize: {
    width: number;
    height: number;
    name: string;
  };
  spacing: number;
}) => {
  // Function implementation
}; 